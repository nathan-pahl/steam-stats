import { OwnedGamesResponse } from './owned-games-response';

describe('OwnedGamesResponse', () => {
  it('should create an instance', () => {
    expect(new OwnedGamesResponse()).toBeTruthy();
  });
});
